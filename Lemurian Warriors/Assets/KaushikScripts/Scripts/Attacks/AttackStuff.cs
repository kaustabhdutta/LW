using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttackStuff : MonoBehaviour
{
    // Start is called before the first frame update
    public enum DamageTypes { None, Fire, Water, Earth, Air};
}
